import React from 'react';
import { Spinner } from '@nextui-org/spinner';

type Props = {};

function Loading({}: Props) {
  return <Spinner className='h-screen w-full md:md:h-h_/w_nav_md' />;
}

export default Loading;
